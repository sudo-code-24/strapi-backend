services:
  db:
    image: postgres:15-alpine
    container_name: jbcmhs_postgres
    restart: always
    env_file: .env
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - "${DATABASE_PORT}:5432"
    volumes:
      # Format: [Host Path]:[Container Path]
      - /media/dev-yol/545fc1c4-f349-4766-903d-090d97e15ab6/projects/docker/postgres_data:/var/lib/postgresql/data
# Removed the top-level 'volumes' section as we are using a direct path
